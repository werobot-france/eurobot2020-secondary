from math import *
from ..API import API

class TestScript(API):
  
  def run(self):
    #self.navigation.orientTo(pi)
    # self.navigation.goTo(x=200, y=950, theta=pi)
    # input('?')
    # self.navigation.goTo(x=-500, y=950, theta=pi, stopOn='front')
    
    
    # 1. Elevator go to TOP
    # self.navigation.goTo(x=750, y=750, theta=pi)
    # self.navigation.goTo(x=300, y=750, theta=radians(210))
    # self.navigation.goTo(x=200, y=1000, theta=pi)
    #self.navigation.goTo(x=770, y=1000, theta=pi)